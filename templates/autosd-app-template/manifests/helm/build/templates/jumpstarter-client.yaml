apiVersion: jumpstarter.dev/v1alpha1
kind: Client
metadata:
  name: {{ .Values.user }}-{{ .Values.app.name }}-build
  namespace: jumpstarter-qemu-exporter # FIXME: do not hardcode namespace
spec:
  username: "kubernetes:{{ .Values.user }}-{{ .Values.app.name }}-build"
